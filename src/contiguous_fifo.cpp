#include "contiguous_fifo.h"

namespace collections {
    template class contiguous_fifo<char>;
    template class contiguous_fifo<int>;
    template class contiguous_fifo<float>;
    template class contiguous_fifo<double>;
}
